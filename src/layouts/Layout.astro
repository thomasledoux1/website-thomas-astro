---
import { ViewTransitions } from "astro:transitions";
// @ts-expect-error
import SpeedInsights from "@vercel/speed-insights/astro";
import Footer from "../components/Footer.astro";

interface Props {
  title?: string;
  description?: string;
  frontmatter?: {
    minutesRead: string;
    title: string;
    url: string;
  };
}
const { frontmatter, title, description } = Astro.props;
import "../../styles/global.css";
---

<html lang="en" class="scroll-smooth">
  <head>
    <ViewTransitions />
    <meta charset="utf-8" />
    <title>{title ?? frontmatter?.title}</title>
    <meta content={title ?? frontmatter?.title} property="og:title" />
    <meta content={title ?? frontmatter?.title} property="twitter:title" />
    <meta name="twitter:card" content="summary_large_image" />
    {
      description ? (
        <>
          <meta content={description} name="description" />
          <meta content={description} property="twitter:description" />
          <meta content={description} property="og:description" />
          <meta name="author" content="Thomas Ledoux" />
        </>
      ) : (
        <>
          <meta
            content="Thomas is a frontend developer from Ghent, Belgium. Specialised in React."
            name="description"
          />
          <meta
            content="Thomas is a frontend developer from Ghent, Belgium. Specialised in React."
            property="twitter:description"
          />
          <meta
            content="Thomas is a frontend developer from Ghent, Belgium. Specialised in React."
            property="og:description"
          />
        </>
      )
    }
    <link
      as="font"
      crossorigin="anonymous"
      href="/fonts/inter-v12-latin-700.woff2"
      rel="preload"
      type="font/woff2"
    />
    <link
      as="font"
      crossorigin="anonymous"
      href="/fonts/inter-v12-latin-regular.woff2"
      rel="preload"
      type="font/woff2"
    />
    <link
      as="font"
      crossorigin="anonymous"
      href="/fonts/CalSans-SemiBold.woff2"
      rel="preload"
      type="font/woff2"
    />
    <meta
      content={frontmatter?.title
        ? `https://thomasledoux.be${frontmatter.url}/og-image.png`
        : "https://res.cloudinary.com/dzrea5zhv/image/upload/v1583171588/me_qvrwky.jpg"}
      property="og:image"
    />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <SpeedInsights />
  </head>
  <body class="font-body">
    <main
      class={`grid min-h-[100dvh] grid-cols-[1rem,1fr,1rem] sm:grid-cols-[1fr,640px,1fr] md:grid-cols-[1fr,768px,1fr] lg:grid-cols-[1fr,1024px,1fr] xl:grid-cols-[1fr,1280px,1fr] 2xl:grid-cols-[1fr,1536px,1fr] h-full pt-8 pb-24`}
    >
      <slot />
    </main>
    <Footer />
    <script>
      document.addEventListener("astro:page-load", () => {
        fetch("/api/update-view", {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
          },
          body: JSON.stringify({
            url: window.location.pathname,
          }),
        });
      });
    </script>
  </body>
</html>
